#include <stdio.h>
#include <stdlib.h>
#include <vector>
#include <queue>
using namespace std;
vector< vector<int> > v;
int n;
void bfs(int u)
{
	queue<int> q[n];
	while(true)
	{
		q.push(u);
		for(std::vector<int>::iterator i = v[u].begin();i!=v[u].end();i++)
		{

		}
	}
}
int main()
{
	int t, m,a;
	scanf("%d",&n);
}